# 增量需求变更

仅在计划展示后，用户改变需求、范围、约束或验收标准时读取。

1. 使旧需求版本及确认状态失效。
2. 标记受影响的任务、文件、接口和验证；未受影响的已完成结果继续复用。
3. 取消尚未开始的受影响任务；中断正在写入相关文件的代理后，才能转移所有权。
4. 向原规划代理发送“旧基线 + 用户差异 + 当前状态”，只要求返回变化部分；简单任务由主代理直接修订。
5. 主代理合并差异，发布新的完整计划和版本号。
6. 用户明确确认新版本后才能恢复受影响开发。

无法安全停止原写入者、差异含义不明确或需要新权限时，停止并询问用户。
